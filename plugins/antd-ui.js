import Vue from 'vue'
import {
  Form,
  Icon,
  Input,
  Button,
  Col,
  Layout,
  Menu,
  Row,
  Table
} from 'ant-design-vue'

const aForm = Form
const aIcon = Icon
const aInput = Input
const aButton = Button
const aCol = Col
const aLayout = Layout
const aMenu = Menu
const aRow = Row
const aTable = Table

Vue.use(aForm)
Vue.use(aIcon)
Vue.use(aInput)
Vue.use(aButton)
Vue.use(aCol)
Vue.use(aLayout)
Vue.use(aMenu)
Vue.use(aRow)
Vue.use(aTable)
